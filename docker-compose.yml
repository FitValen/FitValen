services:
  fitvalen:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      HOSTNAME: 0.0.0.0
      PORT: 3000
    volumes:
      - ./data:/app/data
    ports:
      - "127.0.0.1:3000:3000"
